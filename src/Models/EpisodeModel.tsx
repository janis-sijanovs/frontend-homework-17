export type episodeType = {
    id: number;
    name: string;
    // eslint-disable-next-line camelcase
    air_date: string;
    episode: string;
    characters: string[];
    url: string;
    created: string;
  }
